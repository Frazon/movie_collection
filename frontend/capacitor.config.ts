import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.movie.collection',
  appName: 'Movie Collection',
  webDir: 'dist/frontend/browser',
};

export default config;
